[package]
name = "prefrontal-core"
description = "Prefrontal-RS scanner + config library — shared by prefrontald and the CLI"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
prefrontal-protocol = { workspace = true }
serde = { workspace = true }
anyhow = { workspace = true }
toml = "1.1.4"
dirs = "6.0.0"
gix = "0.86.0"
tantivy = "0.26.1"
